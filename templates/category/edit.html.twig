{% extends 'baseAdministration.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
<section class="banner_area">
      <div class="banner_inner d-flex align-items-center">
        <div class="overlay"></div>
        <div class="container">
          <div class="row justify-content-center">
            <div class="col-lg-6">
              <div class="banner_content text-center">
                <h2>Welcome to Dashborad</h2>
                <div class="page_link">
                  <a href="{{path('index')}}">Home</a>
                  <a href="{{path('admin_category_index')}}">Category</a>
                  <a href="course-details.html">Edit Category </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
</section>  
 <div class="row justify-content-center">
          <div class="col-lg-5">
            <div class="main_title">
              <h1>Edit Category</h1>
              <div class="form-group col-md-6">
                {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}
                 <a href="{{ path('admin_category_index') }}" class="primary-btn ml-sm-4 ml-0">back to list</a>
                <br><br>{{ include('category/_delete_form.html.twig') }}
               </div>
            </div>
          </div>
 </div>
   
{% endblock %}
