{{ form_start(form) }}
   
   
<div class="form-row">
    <div class="form-group col-md-6">
        <i class="fa fa-calendar"></i> 
        {{ form_label(form.titre )  }}
        {{ form_widget(form.titre, { 'attr': { 'class': 'form-control' } }) }}
        <small>{{ form_help(form.titre) }}</small>
        <div class="form-error">
        {{ form_errors(form.titre) }}
        </div>
    </div>
    <div class="form-group col-md-6">

        <i class="fa fa-calendar"></i> 

        {{ form_label(form.auteur) }}
        {{ form_widget(form.auteur , { 'attr': { 'class': 'form-control' } }) }}
        <small>{{ form_help(form.auteur) }}</small>
        <div class="form-error">
            {{ form_errors(form.auteur) }}
        </div>
    </div>
    <div class="form-group col-md-6">
    
        {{ form_label(form.image) }}
        {{ form_widget(form.image, { 'attr': { 'class': 'form-control' } }) }}

        <small>{{ form_help(form.image) }}</small>

        <div class="form-error">
            {{ form_errors(form.image) }}
        </div>
    </div>
     <div class="form-group col-md-6">
        <i class="fa fa-calendar"></i> 
        {{ form_label(form.price) }}
        {{ form_widget(form.price, { 'attr': { 'class': 'form-control' } }) }}

        <small>{{ form_help(form.price) }}</small>

        <div class="form-error">
            {{ form_errors(form.price) }}
        </div>
    </div>
 
    <div class="form-group col-md-6">
 
        <i class="fa fa-calendar"></i>
         {{ form_label(form.resume) }}
         {{ form_widget(form.resume, { 'attr': { 'class': 'form-control' } }) }}
         {{ form_errors(form.resume) }}
        <small>{{ form_help(form.resume) }}</small>
        <div class="form-error">
                {{ form_errors(form.resume) }}
        </div>
    </div>
     <div class="form-group col-md-6">

        <i class="fa fa-calendar"></i> 
        {{ form_label(form.category) }}
        {{ form_widget(form.category, { 'attr': { 'class': 'form-control' } }) }}

        <small>{{ form_help(form.category) }}</small>

        <div class="form-error">
            {{ form_errors(form.category) }}
        </div>
    </div>

   
      <div class="form-group col-md-6">

   <br> <button class="primary-btn ml-sm-4 ml-0">{{ button_label|default('Save') }}</button>
   </div>

</div>
{{ form_end(form) }}
